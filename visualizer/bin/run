#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/../.."

DB_PATH="${1:-examples/pdf_ingestion/progress.db}"

echo "=== Pipetree Visualizer ==="
echo "Database: $DB_PATH"
echo "URL: http://localhost:8000"
echo ""

pipenv run python -m visualizer.app
