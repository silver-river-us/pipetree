#!/usr/bin/env bash
set -euo pipefail

cd "$(dirname "$0")/.."

echo "=== Pipetree Visualizer ==="
echo "URL: http://localhost:8001"
echo ""

pipenv run uvicorn app:app --reload --host 0.0.0.0 --port 8001
